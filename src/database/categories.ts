import "server-only"

import { Categories } from "@/types/categories";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function getCategoriesByName(query: string): Promise<Categories[]> {
  return []
}
