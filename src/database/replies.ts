import "server-only"

import { Replies } from "@/types/replies";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function getRepliesByName(query: string): Promise<Replies[]> {
  return []
}
